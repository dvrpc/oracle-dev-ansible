# {{ app_short_name }}
location {{ url_path }} {
    proxy_pass http://unix:/run/{{ app_short_name }}.sock;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
}	
